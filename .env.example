# Local development only. Copy to .env and change these values for your own machine.
POSTGRES_DB=hessbnb
POSTGRES_USER=hessbnb
POSTGRES_PASSWORD=root
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=local-demo-only
PGADMIN_DEFAULT_EMAIL=admin@example.invalid
PGADMIN_DEFAULT_PASSWORD=local-demo-only
